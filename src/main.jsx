import React from 'react';
import ReactDOM from 'react-dom/client';
import OpenCourt from './OpenCourt.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OpenCourt />
  </React.StrictMode>
);
