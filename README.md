# OpenCourt MVP

## Run locally
```bash
npm install
npm run dev
```

## Deploy quickly
- Vercel: import this folder/repo and deploy
- Netlify: import this folder/repo and use build command `npm run build` and publish directory `dist`
- GitHub Pages: works too, but Vercel is easier for a demo
